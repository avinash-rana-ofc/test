import { InstanceFormValidators } from './instance-form-validators';

describe('InstanceFormValidators', () => {
  it('should create an instance', () => {
    expect(new InstanceFormValidators()).toBeTruthy();
  });
});
